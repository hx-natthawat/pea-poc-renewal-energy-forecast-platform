---
description: "Short alias for /orchestrate - Autonomous SDLC orchestrator"
---

# /one - Quick Orchestrator Alias

This is a short alias for `/orchestrate`. Execute the full orchestrator workflow.

## Instructions

Run the `/orchestrate` command with all its capabilities:

1. **Assess** current project state
2. **Plan** next steps based on priorities
3. **Execute** with specialized subagents
4. **Report** results and next actions

## Arguments

$ARGUMENTS

## Action

Immediately invoke the full orchestrator protocol as defined in `/orchestrate`:

- Read project status (docs/RELEASE-STATUS.md, docs/plans/*.md)
- Check git status for pending changes
- Run tests to verify baseline health
- Identify highest priority work
- Spawn subagents as needed
- Execute tasks autonomously
- Update documentation and knowledge base
- Report results with actionable next steps

Focus on **real production for real users** - not just demos.
